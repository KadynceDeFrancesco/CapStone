//The initial landing screen of the app (not necessarily used after login).
//Typically connected to the storyboard's initial view controller.
//Can serve as a splash or routing screen if desired.

import UIKit

class ViewController: UIViewController {

    override func viewDidLoad() {
        super.viewDidLoad()
    }

}

